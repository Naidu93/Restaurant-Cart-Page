import React from 'react'

const Total = ({ total }) => {
    // console.log(total);

    return (
        <div>
            <h3>Total Amount : {total}</h3>
        </div>
    )
}

export default Total